language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ember-fullstack' ]; then cd .. && eval "mv $currentfolder generator-ember-fullstack" && cd generator-ember-fullstack; fi

